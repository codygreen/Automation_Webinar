{
    "class": "AS3",
    "action": "deploy",
    "persist": true,
    "declaration": {
        "class": "ADC",
        "schemaVersion": "3.2.0",
        "id": "testid",
        "label": "test-label",
        "remark": "test-remark",
        "{{item.split('-')[1].split('_')[0]}}":{
            "class": "Tenant",
            {{ lookup('file', 'tmp/'+item, split_lines=False) }}
        }
    }
}